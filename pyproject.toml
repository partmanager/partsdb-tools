[build-system]
requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]

[project]
name = "partsdb-tools"
dynamic = ["version"]
dependencies = [
    "PyExifTool",
    "opencv-python",
    "rawpy"
]